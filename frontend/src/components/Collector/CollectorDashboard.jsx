import React from 'react';

/**
 * Collector dashboard placeholder (FR-05, FR-06).
 * Owner: Collector team members — covers both Local Collector
 * (pickups, weight recording, bulk lots) and Global Collector (transport).
 * @returns {JSX.Element} The collector dashboard stub.
 */
function CollectorDashboard() {
  return (
    <section>
      <h1>Collector Dashboard</h1>
      <p>Pickup management, held waste, and bulk lots will appear here.</p>
    </section>
  );
}

export default CollectorDashboard;
