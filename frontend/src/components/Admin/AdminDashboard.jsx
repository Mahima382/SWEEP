import React from 'react';

/**
 * Admin portal placeholder (FR-11, FR-12).
 * Owner: Admin team member — extend with user management, KYC queue,
 * pricing, fraud queue, audit logs, and reports.
 * @returns {JSX.Element} The admin dashboard stub.
 */
function AdminDashboard() {
  return (
    <section>
      <h1>Admin Portal</h1>
      <p>User management, KYC queue, and reports will appear here.</p>
    </section>
  );
}

export default AdminDashboard;
