{
  "name": "sweep-e2e",
  "version": "0.1.0",
  "private": true,
  "description": "SWEEP end-to-end tests — Playwright driving the real frontend + backend",
  "scripts": {
    "test": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.1"
  }
}
